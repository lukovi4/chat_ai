{
  "name": "firebase-chat-template",
  "version": "0.1.0",
  "private": true,
  "description": "Deployable Cloud Functions gen2 BFF template for the chat_ai kit: package-owned createChatHandler runtime (Firebase verification, wire validation, idempotency/replay/quota orchestration, OpenAI Responses SSE). One per app. Not yet a deployable endpoint (no Anthropic adapter, app-owned src/index.ts, secrets or project config).",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "firebase-admin": "^14.1.0",
    "openai": "^6.46.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
