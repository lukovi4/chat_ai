{
  "name": "firebase-chat-template",
  "version": "0.1.0",
  "private": true,
  "description": "Foundation of the deployable Cloud Functions gen2 BFF template for the chat_ai kit (contract wire foundation + OpenAI Responses translator). One per app. Not yet a deployable endpoint.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "openai": "^6.46.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
